SECRET_KEY=
DEBUG=True
# Sin DATABASE_URL se usa SQLite; definirla en el entorno para PostgreSQL.
# Formato de referencia: postgresql://<usuario>:<clave>@<host>:<puerto>/<base>
DATABASE_URL=
# Lista separada por comas; Render añade también RENDER_EXTERNAL_HOSTNAME.
ALLOWED_HOSTS=
RENDER_EXTERNAL_HOSTNAME=
